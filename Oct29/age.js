function getAge(inputString){
    // return the girl's correct age as an integer. Happy coding :) 
      return parseInt(inputString[0]);
    }
    
    // we want to create a program where it returns the girls age 0-9
    // the first character will always be a number. 
    // inputString[0] extracts the first character of the string.
    // parseInt converts this character to an integer.