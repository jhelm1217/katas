function greet (name, owner) {
    // Add code here
    return name === owner ? "Hello boss" : "Hello guest";
  }
  
  
  
  // Parameters- there are two, name and owner.
  // It should either say boss or guest. 
  // if the name is the same as the owner then it will say boss, otherwise it will say guest. 